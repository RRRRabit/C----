# 修改计划：删除平滑移动和镜头旋转相关内容

## 一、修改目标

本次修改先做两件清理，再加入少量简单功能：

1. 删除平滑移动相关内容
2. 删除镜头旋转相关内容
3. 增加简单关卡选择
4. 不同关卡使用不同地图大小、速度和目标分数

目标是让代码更接近传统贪吃蛇的写法，减少对初学者来说不太直观的 3D 动画和镜头数学逻辑。

新增功能必须保持简单，不考虑复杂玩法。

代码原则保持不变：

- 写法通俗易懂
- 尽量使用基础语法
- 保持函数职责清楚
- 不引入复杂类和高级写法
- 删除功能时同步删除不用的变量、常量和函数
- 删除后保证项目可以正常编译运行

## 二、目标 1：删除平滑移动

### 当前平滑移动做了什么

现在蛇移动时不是直接画在新格子上，而是通过旧位置和新位置之间的插值，让蛇看起来从一个格子滑到另一个格子。

相关内容包括：

- `oldSnake`
- `SmoothCellToWorld()`
- `GetMoveProgress()`
- `MoveSnake()` 中的 `oldSnake = snake`
- `DrawSnake()` 中用旧位置和新位置计算中间位置

### 计划删除内容

删除以下内容：

```cpp
std::vector<Cell> oldSnake;
```

删除以下函数：

```cpp
Vector3 SmoothCellToWorld(Cell oldCell, Cell newCell, float progress);
float GetMoveProgress();
```

删除 `MoveSnake()` 中保存旧蛇身的代码：

```cpp
oldSnake = snake;
```

修改 `DrawSnake()`：

- 不再计算 `progress`
- 不再读取 `oldSnake`
- 每一节蛇身直接使用当前位置：

```cpp
Vector3 pos = CellToWorld(snake[i]);
```

### 删除后的效果

蛇会恢复成传统贪吃蛇的移动方式：

- 每到一次移动间隔，蛇直接移动到下一格
- 不再有滑行动画
- 代码更短，更容易理解

## 三、目标 2：删除镜头旋转

### 当前镜头旋转做了什么

现在镜头可以通过 `Q / E` 或鼠标右键拖动绕地图旋转，还可以用鼠标滚轮缩放，`C` 键重置视角。

这部分涉及角度、三角函数和摄像机位置计算，对初学者来说不太直观。

相关内容包括：

- `cameraAngle`
- `cameraDistance`
- `cameraHeight`
- `START_CAMERA_DISTANCE`
- `START_CAMERA_HEIGHT`
- `MIN_CAMERA_DISTANCE`
- `MAX_CAMERA_DISTANCE`
- `CAMERA_ROTATE_SPEED`
- `MOUSE_ROTATE_SPEED`
- `CAMERA_ZOOM_SPEED`
- `UpdateCameraRotation()`
- `UpdateCameraPosition()` 中使用 `sinf()` 和 `cosf()` 计算摄像机绕圈位置
- `ResetCameraView()`

### 计划删除内容

删除镜头旋转、缩放、重置相关变量和常量。

删除以下函数：

```cpp
void UpdateCameraRotation();
void ResetCameraView();
```

修改 `UpdateCameraPosition()`：

- 不再根据角度计算摄像机位置
- 改成固定视角

建议固定摄像机位置：

```cpp
camera.position = (Vector3){0.0f, 22.0f, 24.0f};
camera.target = (Vector3){0.0f, 0.0f, 0.0f};
camera.up = (Vector3){0.0f, 1.0f, 0.0f};
camera.fovy = 45.0f;
camera.projection = CAMERA_PERSPECTIVE;
```

修改 `main()`：

- 初始化时直接调用 `UpdateCameraPosition()`
- 主循环中删除 `UpdateCameraRotation()`

修改 `DrawUI()` 和 `README.md`：

- 删除 `Q / E` 旋转视角提示
- 删除鼠标右键拖动旋转提示
- 删除鼠标滚轮缩放提示
- 删除 `C` 重置视角提示

### 删除后的效果

镜头会固定在斜上方俯视地图。

玩家只需要关注蛇的移动，不需要理解或操作 3D 镜头。

## 四、目标 3：增加简单关卡选择

### 为什么加这个功能

关卡选择实现起来比较简单，但效果明显。

玩家可以在游戏开始前选择不同关卡，不同关卡对应不同地图大小、移动速度和目标分数。

这样项目看起来更完整，但代码仍然好理解。

### 计划新增关卡

建议只做 3 个关卡：

| 关卡 | 地图大小 | 初始速度 | 目标分数 |
|------|----------|----------|----------|
| 简单 | 15 x 15 | 慢 | 50 |
| 普通 | 20 x 20 | 正常 | 100 |
| 困难 | 25 x 25 | 快 | 150 |

注意：这里的“速度”仍然通过移动间隔控制，移动间隔越小，蛇越快。

### 计划新增变量

把原来的固定地图大小改成变量：

```cpp
int gridSize;
```

新增当前关卡：

```cpp
int currentLevel;
int targetScore;
float startMoveInterval;
float minMoveInterval;
float speedUpAmount;
```

建议保留 `GRID_SIZE` 作为默认值或删除它，后续统一使用 `gridSize`。

### 计划新增函数

```cpp
void SetLevel(int level);
void HandleLevelSelectInput();
void DrawLevelSelectUI();
```

函数作用：

- `SetLevel()`：根据关卡编号设置地图大小、速度和目标分数
- `HandleLevelSelectInput()`：在选关界面读取按键 `1 / 2 / 3`
- `DrawLevelSelectUI()`：绘制简单的选关提示

### 游戏状态调整

当前 `GameStatus` 可以增加一个状态：

```cpp
LevelSelect
```

游戏启动后先进入选关界面。

玩家按：

- `1` 选择简单
- `2` 选择普通
- `3` 选择困难

选择后调用：

```cpp
SetLevel(level);
InitGame();
```

然后进入正常游戏。

## 五、目标 4：目标分数通关

### 当前胜利条件

现在的胜利条件是蛇占满整个地图。

这个目标很难达到，而且对普通玩家来说不够直观。

### 修改后的胜利条件

每个关卡设置目标分数。

当分数达到目标分数时，当前关卡胜利。

例如：

```cpp
if (score >= targetScore) {
    gameStatus = Win;
}
```

这样胜利更容易测试，也更适合课程设计展示。

### UI 显示

分数区域增加目标分数显示：

```text
Target: 100
```

胜利提示中显示当前关卡和最终分数。

## 六、建议修改顺序

### 第一步：删除平滑移动

先删除 `oldSnake` 和插值逻辑。

原因：

- 改动范围比较集中
- 删除后容易检查蛇是否还能正常移动

### 第二步：删除镜头旋转

再删除镜头旋转和缩放逻辑。

原因：

- 镜头逻辑独立于蛇移动
- 删除后只需要确认视角固定且能看清地图

### 第三步：同步更新说明文档

先把删除的按键说明从 `README.md` 中去掉。

原因：

- 避免文档里还保留已经删除的按键说明

### 第四步：增加关卡选择

加入 `LevelSelect` 状态、`SetLevel()`、`HandleLevelSelectInput()` 和 `DrawLevelSelectUI()`。

原因：

- 关卡选择独立于蛇移动
- 逻辑简单，适合放在删除复杂逻辑之后

### 第五步：增加目标分数通关

在吃到食物加分后判断是否达到 `targetScore`。

原因：

- 只需要在已有分数逻辑上增加判断
- 不需要改复杂游戏结构

### 第六步：再次同步 README

补充选关说明、关卡表和目标分数规则。

## 七、测试计划

修改完成后需要测试：

- 项目可以正常编译
- 游戏窗口可以正常打开
- 蛇可以用 `W / A / S / D` 或方向键移动
- 蛇移动时直接跳到下一格，不再滑动
- 吃食物、加分、变长正常
- 撞墙和撞自己仍然失败
- 镜头固定，不再响应 `Q / E`
- 镜头固定，不再响应鼠标右键拖动
- 镜头固定，不再响应鼠标滚轮缩放
- `C` 键不再作为重置视角使用
- UI 和 README 中没有残留镜头旋转说明
- 游戏启动后显示选关界面
- 按 `1` 进入简单关卡，地图大小为 `15 x 15`
- 按 `2` 进入普通关卡，地图大小为 `20 x 20`
- 按 `3` 进入困难关卡，地图大小为 `25 x 25`
- 不同关卡的速度有明显区别
- UI 显示目标分数
- 分数达到目标分数后进入胜利状态

## 八、本次不做的内容

本次只加入简单、好理解、容易展示的功能。

不做：

- 不修改蛇头眼睛
- 不删除食物特效
- 不删除棋盘格地图
- 不删除 UI 居中提示
- 不调整最高分逻辑
- 不加入障碍物系统
- 不加入特殊食物
- 不加入限时食物
- 不加入上下层 3D 移动
- 不加入跳跃、坡道、重力
- 不加入复杂菜单系统
- 不加入多关卡连续推进
- 不加入存档系统
- 不加入音效系统

复杂玩法全部不考虑，避免代码变难读。

## 九、预期结果

修改后代码会更直接：

- 蛇的位置就是当前格子位置
- 绘制时直接把格子转换成 3D 坐标
- 摄像机固定在一个位置
- 玩家操作更接近传统贪吃蛇
- 初学者阅读时少接触插值动画和三角函数镜头计算
- 游戏有简单选关界面
- 地图大小、速度和目标分数会随关卡变化
- 项目看起来更完整，但代码仍然保持基础写法
