# 修改计划：解决主菜单画面布局问题

## 一、当前截图中的问题

从截图看，主菜单已经能正常显示和点击，但画面布局还不够协调，主要问题如下：

1. 菜单内容整体没有形成一个完整区域  
   标题在中间，但关卡按钮、速度、难度和开始按钮分散在不同位置，看起来不够统一。

2. 右侧空白太多  
   当前窗口比较大，但菜单内容主要集中在左中区域，视觉重心偏左。

3. 速度和难度调节行不整齐  
   `-`、`+` 和中间文字的位置关系不清楚，尤其是难度目标文字离按钮太近，容易显得重合。

4. 主菜单缺少统一背景面板  
   所有控件直接放在背景上，层次感不够明显。

5. 坐标分散，不方便后续调整  
   当前按钮坐标是分别写死的，改一个位置时容易忘记同步点击区域。

## 二、修改目标

本次只优化主菜单布局，不新增复杂功能。

目标：

- 菜单整体居中
- 所有主菜单控件放进一个统一面板
- 地图关卡按钮排列整齐
- 速度调节和难度调节改成清晰的一行结构
- `-`、文字、`+` 三者之间留足距离
- 开始按钮放在面板底部居中
- 绘制坐标和点击坐标保持一致
- 代码保持简单，继续用基础 `Rectangle` 和鼠标点击判断

## 三、推荐布局

窗口保持当前大窗口。

主菜单使用一个固定面板，例如：

```cpp
int menuX = 560;
int menuY = 170;
int menuWidth = 800;
int menuHeight = 720;
```

标题：

```text
SNAKE MENU
```

标题放在面板上方居中。

### 地图关卡区域

地图区域放在面板内部上半部分。

推荐结构：

```text
Choose Map

[ Level 1 ]  [ Level 2 ]  [ Level 3 ]  [ Level 4 ]
  10 x 10      15 x 15      20 x 20      25 x 25
```

要求：

- 四个按钮等宽
- 四个按钮间距一致
- 地图大小文字在按钮正下方居中

### 速度调节区域

速度区域改成一整行：

```text
Speed

[ - ]        Level 3        [ + ]
```

要求：

- `-` 按钮在左
- `+` 按钮在右
- `Level 3` 在中间居中
- 中间文字不要贴近按钮

### 难度目标区域

难度目标区域也改成一整行：

```text
Difficulty Target

[ - ]    Level 3  Target 60    [ + ]
```

要求：

- 不允许 `+` 和文字重合
- 不允许文字压到按钮边框
- 中间文字整体居中

### 开始按钮

开始按钮放在最下方：

```text
[ START ]
```

要求：

- 按钮居中
- 比普通按钮稍宽
- 和上方难度区域保持足够距离

## 四、代码实现原则

为了保持代码好读，不使用复杂 UI 系统。

继续使用：

```cpp
Rectangle button;
DrawButton(button, text, selected);
IsButtonClicked(button);
```

不要加入：

- 自适应布局系统
- 拖动条
- 下拉框
- 多页面菜单
- 图片按钮
- 复杂动画

## 五、建议新增简单辅助函数

为了避免绘制坐标和点击坐标不一致，建议把菜单按钮位置统一写成函数。

例如：

```cpp
Rectangle GetLevelButtonRect(int index);
Rectangle GetSpeedMinusButtonRect();
Rectangle GetSpeedPlusButtonRect();
Rectangle GetDifficultyMinusButtonRect();
Rectangle GetDifficultyPlusButtonRect();
Rectangle GetStartButtonRect();
```

这样：

- `HandleLevelSelectInput()` 用这些函数判断点击
- `DrawLevelSelectUI()` 也用这些函数绘制按钮

这样代码更长一点，但更不容易出错，也更适合初学者理解。

## 六、建议修改顺序

### 第一步：整理菜单固定坐标

在 `DrawLevelSelectUI()` 和 `HandleLevelSelectInput()` 附近统一设置菜单区域坐标。

先确定：

```cpp
menuX
menuY
menuWidth
menuHeight
```

### 第二步：增加按钮坐标函数

先写出各个按钮的 `Rectangle` 返回函数。

然后把绘制和点击判断都改成调用这些函数。

### 第三步：重画主菜单

按以下顺序绘制：

1. 标题
2. 半透明菜单面板
3. 地图关卡按钮
4. 速度调节行
5. 难度目标调节行
6. 开始按钮

### 第四步：同步点击逻辑

确认所有按钮点击区域和绘制区域完全一致。

### 第五步：编译运行检查

运行 `build.bat`，确认没有编译错误。

## 七、测试计划

修改完成后测试：

- 主菜单整体居中
- 右侧不再显得大面积空白
- 四个地图关卡按钮排列整齐
- 点击 Level 1 到 Level 4 都能正确切换
- 速度 `-` 和 `+` 能正常点击
- 难度目标 `-` 和 `+` 能正常点击
- 速度文字不和按钮重合
- 难度目标文字不和按钮重合
- START 按钮能正常开始游戏
- 游戏中按 `M` 返回主页面后布局仍正常
- 编译成功，`snake.exe` 可以运行

## 八、本次不做的内容

本次只解决截图中的主菜单布局问题。

不做：

- 不改游戏规则
- 不改地图关卡数量
- 不改速度和难度计算方式
- 不重新加入全屏功能
- 不做复杂自适应缩放
- 不添加新页面
- 不添加动画效果

## 九、预期效果

修改后主菜单应该看起来像一个完整的游戏菜单：

- 标题居中
- 菜单区域集中
- 按钮整齐
- 信息不重叠
- 视觉重心更平衡
- 代码仍然是基础函数和简单坐标，适合初学者阅读
