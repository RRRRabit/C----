# 贪吃蛇代码双人阅读分工

## A 同学负责

### 1. `crazy_mode.cpp` 前半部分

- `UpdateCrazyMode()`
- `UpdateBoostEvent()`
- `UpdateBlueBlocks()`

对应头文件：

- `crazy_mode.h` 中对应函数声明

### 2. `snake_logic.cpp` 前半基础部分

- `InitGame()`
- `StartSelectedGame()`
- `UpdateGame()`
- `UpdateEffects()`
- `IsSameCell()`
- `IsCellOnSnake()`
- `IsCellOnCrazyBlock()`
- `IsCellOnSolidCrazyBlock()`

对应头文件：

- `snake_logic.h` 中对应函数声明

### 3. `main.cpp`

- `InitProgram()`
- `UpdateProgram()`
- `DrawProgram()`
- `CloseProgram()`
- `main()`

### 4. `settings.cpp`

- `GetMenuX()`
- `GetMapSizeByLevel()`
- `GetLevelName()`
- `SetLevel()`
- `SetSpeedByLevel()`
- `UpdateTargetScore()`
- `SetDefaultSettings()`

对应头文件：

- `settings.h`

### 5. `loading.cpp`

- `StartLoading()`
- `UpdateLoading()`
- `IsLoading()`
- `DrawLoadingScreen()`

对应头文件：

- `loading.h`

### 6. `score_file.cpp`

- `LoadHighScore()`
- `SaveHighScore()`
- `UpdateHighScore()`

对应头文件：

- `score_file.h`

### 7. `draw_ui.cpp`

- `GetLevelButtonRect()`
- `GetSpeedMinusButtonRect()`
- `GetSpeedPlusButtonRect()`
- `GetDifficultyMinusButtonRect()`
- `GetDifficultyPlusButtonRect()`
- `GetCrazyModeButtonRect()`
- `GetStartButtonRect()`
- `IsButtonClicked()`
- `DrawButton()`
- `DrawLevelSelectUI()`
- `DrawUI()`
- `DrawCenterMessage()`

对应头文件：

- `draw_ui.h`

## B 同学负责

### 1. `crazy_mode.cpp` 后半部分

- `TryStartCrazyEvent()`
- `TriggerRandomCrazyEvent()`
- `StartBoostEvent()`
- `StartBlueBlockEvent()`
- `IsBoostEventBusy()`

对应头文件：

- `crazy_mode.h` 中对应函数声明

### 2. `snake_logic.cpp` 碰撞和后半部分

- `CheckWallCollision()`
- `CheckSelfCollision()`
- `CanPassWall()`
- `GetNextHead()`
- `WrapHeadIfNeeded()`
- `CheckGameOver()`
- `EatFoodIfNeeded()`
- `UpdateSnakeLength()`
- `MoveSnake()`
- `SpawnFood()`
- `GetCurrentMoveInterval()`

对应头文件：

- `snake_logic.h` 中对应函数声明

### 3. `types.h`

- `Cell`
- `CrazyBlock`
- `GameStatus`
- `SpeedSetting`
- `GameSettings`
- `GameData`
- `EffectData`
- `CrazyModeData`
- `AssetData`
- `LoadingData`
- `GameContext`

### 4. `constants.h`

- 窗口常量
- 地图和关卡常量
- 菜单设置范围常量
- 分数和成长常量
- 文件路径常量
- 3D 绘制常量
- 简单特效常量
- 疯狂模式常量
- Loading 过渡常量
- 主菜单布局常量

### 5. `input.cpp`

- `HandleInput()`
- `HandleLevelSelectInput()`
- `TryChangeDirection()`

对应头文件：

- `input.h`

### 6. `draw_scene.cpp`

- `UpdateCameraPosition()`
- `CellToWorld()`
- `GetSnakeColor()`
- `DrawGame3D()`
- `DrawGridGround()`
- `DrawWalls()`
- `DrawSnake()`
- `DrawSnakeHead()`
- `DrawSnakeBody()`
- `DrawFood()`
- `DrawEatEffect()`
- `DrawCrazyBlocks()`

对应头文件：

- `draw_scene.h`

### 7. `assets.cpp`

- `LoadAssets()`
- `UnloadAssets()`
- `DrawBackgroundImage()`

对应头文件：

- `assets.h`
