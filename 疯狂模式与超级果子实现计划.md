# 疯狂模式与超级果子实现计划

## 一、目标概述

在现有 3D 贪吃蛇基础上新增 `Crazy Mode` 开关。

开启后加入三类疯狂模式规则：

1. 边界穿越  
   蛇从地图一边出去，会从另一边出现。

2. 红光加速  
   蛇可能会闪烁几下红光，然后加速 5 秒，速度变为 1.5 倍。

3. 蓝光实体方块  
   地图中的部分格子可能会闪烁几下蓝光，之后变成实体障碍，蛇撞到会失败。

同时新增“超级果子”：

- 普通果子：增长 1 节，分数 +10
- 超级果子：增长 5 节，分数 +50
- 超级果子不只属于疯狂模式，普通模式也会出现

代码原则保持不变：

- 写法通俗易懂
- 尽量使用基础语法
- 保持单文件为主
- 多用普通函数
- 不使用复杂类结构
- 不加入音效、复杂粒子、复杂动画或新页面

## 二、主菜单新增内容

主菜单增加一个疯狂模式开关按钮：

```text
Crazy Mode: ON / OFF
```

新增变量：

```cpp
bool crazyMode;
```

规则：

- 点击按钮切换 `crazyMode`
- `crazyMode == false` 时是普通模式
- `crazyMode == true` 时启用边界穿越、红光加速、蓝光实体方块
- 返回主菜单后，疯狂模式开关保持当前选择

推荐新增函数：

```cpp
Rectangle GetCrazyModeButtonRect();
```

需要修改：

- `DrawLevelSelectUI()`：绘制疯狂模式按钮
- `HandleLevelSelectInput()`：处理疯狂模式按钮点击

## 三、超级果子

### 规则

每次生成食物时，有固定概率生成超级果子。

建议概率：

```cpp
const int SUPER_FOOD_CHANCE = 20;
```

含义：

- `20` 表示 20% 概率生成超级果子
- 其余 80% 生成普通果子

新增变量：

```cpp
bool isSuperFood;
```

吃到普通果子：

```cpp
score += 10;
growCount = 1;
```

吃到超级果子：

```cpp
score += 50;
growCount = 5;
```

### 实现方式

当前蛇吃到食物后，是“不移除尾巴”来增长 1 节。

为了让超级果子增长 5 节，推荐新增变量：

```cpp
int extraGrowCount;
```

逻辑：

- 吃普通果子：`extraGrowCount += 1`
- 吃超级果子：`extraGrowCount += 5`
- 每次移动时，如果 `extraGrowCount > 0`，就不删除尾巴，并让 `extraGrowCount--`
- 如果 `extraGrowCount == 0`，正常删除尾巴

这样代码比一次性插入 5 节更容易理解。

### 绘制

修改 `DrawFood()`：

- 普通果子：继续使用红色球体
- 超级果子：使用金色或紫色球体

推荐：

```cpp
if (isSuperFood) {
    DrawSphere(pos, CUBE_SIZE * 0.5f, GOLD);
} else {
    DrawSphere(pos, CUBE_SIZE * 0.4f, RED);
}
```

## 四、疯狂模式：边界穿越

### 普通模式

普通模式保持原规则：

- 撞墙失败

### 疯狂模式

疯狂模式下，蛇头越界后从另一侧出现。

示例：

```cpp
if (newHead.x < 0) {
    newHead.x = gridSize - 1;
}
if (newHead.x >= gridSize) {
    newHead.x = 0;
}
if (newHead.z < 0) {
    newHead.z = gridSize - 1;
}
if (newHead.z >= gridSize) {
    newHead.z = 0;
}
```

修改位置：

- `MoveSnake()`

推荐做法：

- 先计算 `newHead`
- 如果 `crazyMode == true`，先处理穿越
- 如果 `crazyMode == false`，再检查撞墙

## 五、疯狂模式：红光加速

### 规则

疯狂模式中，随机触发红光加速事件。

触发后：

- 蛇闪烁红光
- 速度变为 1.5 倍
- 持续 5 秒
- 时间结束后恢复正常速度

新增变量：

```cpp
float crazyEventTimer;
float boostTimer;
float redFlashTimer;
```

推荐常量：

```cpp
const float BOOST_TIME = 5.0f;
const float RED_FLASH_TIME = 1.0f;
const float BOOST_SPEED_RATE = 1.5f;
```

### 加速逻辑

修改 `GetCurrentMoveInterval()`：

```cpp
if (boostTimer > 0.0f) {
    interval = interval / BOOST_SPEED_RATE;
}
```

因为移动间隔越小，蛇越快。

### 红光闪烁

修改 `DrawSnakeHead()` 和 `DrawSnakeBody()`：

- 如果 `redFlashTimer > 0`
- 使用 `GetTime()` 做简单闪烁判断
- 闪烁时把蛇颜色临时改成红色

示例思路：

```cpp
bool showRed = redFlashTimer > 0.0f && ((int)(GetTime() * 10) % 2 == 0);
```

## 六、疯狂模式：蓝光实体方块

### 规则

疯狂模式中，随机选择地图中的空格子，让它闪烁蓝光。

闪烁结束后：

- 变成实体障碍
- 蛇撞到实体蓝方块后失败
- 食物不会生成在蓝方块上

新增结构：

```cpp
struct CrazyBlock {
    Cell cell;
    bool solid;
    float flashTimer;
};
```

新增变量：

```cpp
std::vector<CrazyBlock> crazyBlocks;
```

推荐常量：

```cpp
const float BLUE_FLASH_TIME = 1.5f;
const int MAX_CRAZY_BLOCKS = 8;
```

### 新增函数

```cpp
bool IsCellOnCrazyBlock(Cell cell);
bool IsCellOnSolidCrazyBlock(Cell cell);
void StartBlueBlockEvent();
void DrawCrazyBlocks();
```

### 蓝方块生成规则

生成蓝方块时，需要避开：

- 蛇身
- 当前食物
- 已经存在的蓝方块

如果蓝方块数量达到上限，不再继续生成。

## 七、疯狂事件触发方式

疯狂模式使用随机定时触发。

新增函数：

```cpp
void UpdateCrazyMode();
void TriggerRandomCrazyEvent();
void StartBoostEvent();
void StartBlueBlockEvent();
```

建议逻辑：

```cpp
crazyEventTimer -= GetFrameTime();

if (crazyEventTimer <= 0.0f) {
    TriggerRandomCrazyEvent();
    crazyEventTimer = 4.0f + (rand() % 5);
}
```

`TriggerRandomCrazyEvent()` 中随机选择：

- 红光加速
- 蓝光方块

建议初版各占 50%。

## 八、需要修改的现有函数

### `InitGame()`

需要清空：

```cpp
crazyEventTimer = 3.0f;
boostTimer = 0.0f;
redFlashTimer = 0.0f;
crazyBlocks.clear();
extraGrowCount = 0;
```

注意：不要在 `InitGame()` 中把 `crazyMode` 强制改成 `false`。

### `SpawnFood()`

需要避开蓝方块：

```cpp
if (!IsCellOnSnake(cell) && !IsCellOnCrazyBlock(cell)) {
    emptyCells.push_back(cell);
}
```

并随机设置：

```cpp
isSuperFood = (rand() % 100 < SUPER_FOOD_CHANCE);
```

### `MoveSnake()`

需要处理：

- 疯狂模式边界穿越
- 实体蓝方块碰撞
- 普通果子和超级果子的不同增长

### `UpdateEffects()`

可以在这里更新：

- `boostTimer`
- `redFlashTimer`
- 蓝方块 `flashTimer`

也可以新增 `UpdateCrazyMode()`，并在主循环中调用。

推荐使用 `UpdateCrazyMode()`，逻辑更清楚。

### `DrawGame3D()`

增加：

```cpp
DrawCrazyBlocks();
```

建议放在 `DrawFood()` 前后都可以，推荐放在食物前：

```cpp
DrawGridGround();
DrawWalls();
DrawCrazyBlocks();
DrawFood();
DrawEatEffect();
DrawSnake();
```

## 九、测试计划

### 普通模式测试

- 撞墙仍然失败
- 不触发红光加速
- 不生成蓝光方块
- 超级果子可以出现
- 吃超级果子后增长 5 节

### 疯狂模式测试

- 开启 Crazy Mode 后开始游戏
- 四个边界都可以穿越
- 红光事件触发后速度明显变快
- 5 秒后速度恢复正常
- 蓝光方块先闪烁，再变成实体
- 撞到实体蓝方块后失败
- 食物不会生成在蓝方块上

### 超级果子测试

- 超级果子颜色明显不同
- 吃到后分数 +50
- 蛇身增长 5 节
- 达到目标分数后正常胜利

### 编译测试

运行：

```bat
build.bat
```

确认：

- 编译成功
- `snake.exe` 可以启动
- 主菜单可以点击
- 游戏中不崩溃

## 十、本次不做的内容

本次不做：

- 不加音效
- 不加复杂粒子
- 不加复杂动画
- 不加新页面
- 不加存档
- 不加更多道具
- 不重新加入全屏功能

## 十一、预期效果

实现后游戏会有两个层次：

普通模式：

- 规则接近传统贪吃蛇
- 但有超级果子增加惊喜

疯狂模式：

- 可以穿墙
- 会随机加速
- 地图会随机出现危险实体方块
- 游戏节奏更刺激

整体代码仍然保持基础写法，适合课程设计展示和初学者阅读。
