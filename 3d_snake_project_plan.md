# 3D 贪吃蛇项目规划

## 1. 项目简介

本项目使用 **C++ + raylib** 实现一个 3D 贪吃蛇小游戏。

玩家控制一条由 3D 方块组成的蛇，在固定大小的地图中移动。蛇吃到食物后身体变长，分数增加；如果撞到墙壁或撞到自己的身体，则游戏结束。

本项目适合作为 C++ 课程设计，重点是功能完整、代码清楚、便于初学者理解和答辩讲解。

## 2. 开发原则

本项目代码写法要尽量适合初学者阅读，尽量使用基础、直观、容易讲清楚的方法。

具体原则如下：

- 尽量使用基础 C++ 写法。
- 不使用复杂类继承。
- 不使用模板、智能指针、复杂设计模式。
- 不使用复杂工程结构。
- 使用简单的 `struct` 保存数据。
- 使用 `std::vector` 保存蛇的身体。
- 函数命名要直观，能直接看出作用。
- 游戏逻辑和绘制逻辑分开写。
- 每个函数只完成一个明确任务。
- 关键位置添加简单注释，方便理解。

## 3. 技术方案

项目使用 raylib 作为图形库。

选择 raylib 的原因：

- 写法比直接使用 OpenGL 更简单。
- 可以直接创建窗口。
- 可以直接绘制 3D 方块。
- 可以直接读取键盘输入。
- 适合 C++ 初学者做 3D 小游戏。
- 代码量比直接使用 OpenGL 少很多。

开发环境：

- 编程语言：C++
- 图形库：raylib
- 编译器：MinGW g++
- 构建方式：`build.bat`
- 项目类型：Windows 桌面小游戏

## 4. 游戏功能

第一版实现以下功能：

- 创建 3D 游戏窗口。
- 显示 3D 地面网格。
- 显示由方块组成的蛇。
- 显示随机生成的食物。
- 支持蛇自动移动。
- 支持方向键和 WASD 控制方向。
- 吃到食物后蛇身增长。
- 吃到食物后分数增加。
- 撞墙后游戏结束。
- 撞到自己后游戏结束。
- 支持暂停和继续。
- 支持重新开始游戏。
- 显示当前分数和游戏状态。

## 5. 项目文件结构

推荐文件结构如下：

```text
3d_snake/
├── main.cpp
├── build.bat
├── README.md
└── third_party/
    └── raylib/
        ├── include/
        └── lib/
```

文件说明：

- `main.cpp`：游戏主要代码。
- `build.bat`：一键编译脚本。
- `README.md`：项目介绍、运行方法、按键说明。
- `third_party/raylib`：存放 raylib 头文件和库文件。

## 6. 核心数据设计

蛇在地图中移动时，使用整数方格坐标表示位置。

```cpp
struct Cell {
    int x;
    int z;
};
```

说明：

- `x` 表示地图横向位置。
- `z` 表示地图纵向位置。
- 不使用 `y` 作为移动方向。
- `y` 轴只用于 3D 显示高度。

游戏状态使用简单枚举：

```cpp
enum GameStatus {
    Playing,
    Paused,
    GameOver
};
```

蛇身体使用：

```cpp
std::vector<Cell> snake;
```

当前移动方向使用：

```cpp
Cell direction;
```

食物位置使用：

```cpp
Cell food;
```

## 7. 主要函数设计

建议使用以下函数组织代码：

```cpp
void InitGame();
void UpdateGame();
void DrawGame();

void HandleInput();
void MoveSnake();
void SpawnFood();

bool IsSameCell(Cell a, Cell b);
bool IsCellOnSnake(Cell cell);
bool CheckWallCollision(Cell head);
bool CheckSelfCollision(Cell head);

Vector3 CellToWorld(Cell cell);
```

函数说明：

- `InitGame()`：初始化游戏数据。
- `UpdateGame()`：更新游戏逻辑。
- `DrawGame()`：绘制游戏画面。
- `HandleInput()`：处理键盘输入。
- `MoveSnake()`：让蛇移动一格。
- `SpawnFood()`：生成新的食物。
- `IsSameCell()`：判断两个格子是否相同。
- `IsCellOnSnake()`：判断某个格子是否在蛇身上。
- `CheckWallCollision()`：判断是否撞墙。
- `CheckSelfCollision()`：判断是否撞到自己。
- `CellToWorld()`：把地图格子转换为 3D 坐标。

## 8. 游戏逻辑流程

程序运行流程：

```text
初始化窗口
初始化摄像机
初始化游戏数据

只要窗口没有关闭：
    读取键盘输入
    如果游戏正在进行：
        按固定时间间隔移动蛇
        判断是否吃到食物
        判断是否撞墙
        判断是否撞到自己

    绘制 3D 场景
    绘制蛇
    绘制食物
    绘制分数和提示文字

关闭窗口
```

## 9. 默认参数

第一版使用以下默认参数：

```cpp
const int SCREEN_WIDTH = 1000;
const int SCREEN_HEIGHT = 700;

const int GRID_SIZE = 20;
const float CUBE_SIZE = 1.0f;

const float MOVE_INTERVAL = 0.18f;
```

说明：

- 窗口大小为 `1000 x 700`。
- 地图大小为 `20 x 20`。
- 每个方块大小为 `1.0`。
- 蛇每 `0.18` 秒移动一次。

## 10. 按键设计

```text
W / 上方向键：向前移动
S / 下方向键：向后移动
A / 左方向键：向左移动
D / 右方向键：向右移动

Space：暂停 / 继续
R：重新开始
Esc：退出游戏
```

## 11. 绘制方案

3D 画面采用简单清晰的绘制方式：

- 地面使用网格线显示地图范围。
- 蛇头使用一种颜色。
- 蛇身使用另一种颜色。
- 食物使用醒目的红色。
- 游戏结束时在屏幕中间显示提示文字。

蛇和食物都可以用 raylib 的基础绘图函数实现：

```cpp
DrawCube();
DrawCubeWires();
DrawSphere();
```

这样代码更容易理解，不需要加载模型、贴图或复杂材质。

## 12. 碰撞规则

游戏需要判断三种情况：

1. 蛇头和食物位置相同。
   结果：分数增加，蛇身变长，重新生成食物。
2. 蛇头超出地图边界。
   结果：游戏结束。
3. 蛇头碰到蛇身体。
   结果：游戏结束。

为了方便理解，所有碰撞都基于 `Cell` 方格坐标判断，不直接用复杂的 3D 碰撞检测。

## 13. 测试内容

项目完成后需要测试：

- 程序能正常编译。
- 程序能正常打开窗口。
- 3D 场景能正确显示。
- 蛇能自动移动。
- 方向键和 WASD 能控制方向。
- 蛇不能直接反向移动。
- 食物能随机生成。
- 食物不会生成在蛇身上。
- 吃到食物后分数增加。
- 吃到食物后蛇身变长。
- 撞墙后游戏结束。
- 撞到自己后游戏结束。
- 暂停功能正常。
- 重新开始功能正常。
- 退出功能正常。

## 14. 后续扩展

基础功能完成后，可以继续扩展：

- 增加开始菜单。
- 增加最高分保存。
- 增加音效。
- 增加背景音乐。
- 增加障碍物。
- 增加不同难度。
- 增加速度随分数提升。
- 增加更好看的 3D 效果。
- 增加相机视角切换。

## 15. 实施顺序

推荐开发顺序：

1. 准备 raylib 依赖。
2. 创建 `main.cpp`。
3. 创建窗口和摄像机。
4. 绘制 3D 地面。
5. 绘制静态蛇和食物。
6. 实现蛇自动移动。
7. 实现键盘控制。
8. 实现吃食物和增长。
9. 实现撞墙检测。
10. 实现撞自己检测。
11. 实现分数显示。
12. 实现暂停和重新开始。
13. 编写 `README.md`。
14. 测试并整理代码。

## 16. 默认假设

本项目默认采用以下方案：

- 使用 C++ 实现。
- 使用 raylib 实现 3D 显示。
- 使用 MinGW g++ 编译。
- 不使用 CMake。
- 不使用复杂面向对象设计。
- 第一版先做单人模式。
- 第一版不加入复杂模型和贴图。
- 第一版重点放在功能完整和代码易懂。
